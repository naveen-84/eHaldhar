import 'package:flutter/material.dart';

class CropProtectionPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Crop Protection")),
      body: Center(child: Text("Crop Protection Content Goes Here")),
    );
  }
}
