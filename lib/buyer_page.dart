import 'package:flutter/material.dart';

class BuyerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Buyer")),
      body: Center(
        child: Text("Buyer Page Content Here"),
      ),
    );
  }
}
