import 'package:flutter/material.dart';

class CropCarePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Crop Care")),
      body: Center(child: Text("Crop Care Content Goes Here")),
    );
  }
}
